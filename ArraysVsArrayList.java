package com.collection.framework;

public class ArraysVsArrayList 
{
	public static void main(String...args)
	{
		/*------------------Arrays----------------------------
		 * Static in nature--size fixed
		 * size of the array can be identified by length attribute
		 * arrays can be one two multi dimensional
		 * array does not support generics
		 * array give runtime exception ArrayStoreException if element is incompatible 
		 * array  can hold both object and primitive types
		 * add element by assignment operator
		 * iterate the element using for loop for-each loop 
		 * 
		 */
		
		
		
		/*---------------------------ArrayList-------------------
		 * dynamic in nature
		 * support generics
		 * compile time checking
		 * size() to identify the size of the array
		 * add() to add the element
		 * iterate element by for for-each loop and iterator()
		 * array hold only object if it is primitive it boxed into object
		 * arraylist one dimensional
		 * 
		 */
	}

}
